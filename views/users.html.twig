{% extends "layout.html.twig" %}

{% block content %}
    <div class="row">
        <div class="col m12 s12 l12">
            <div class="collection">
                {% for user in users %}
                    <a href="/user/{{ user.userId }}" class="collection-item">{{ user.userLogin }}</a>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}